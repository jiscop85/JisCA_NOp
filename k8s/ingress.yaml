apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jisca-nop-ingress
  namespace: jisca-nop
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
